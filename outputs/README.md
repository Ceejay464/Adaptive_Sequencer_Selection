# Validation records

Both notebooks ran all 17 code cells in fresh local Python processes, with their actual outputs recorded in display order. The local suite passed 17 model and presentation tests, including 27 Bayesian parameter combinations. KaTeX typeset 48 mathematical expressions in strict mode; Mermaid parsed all three README diagrams. Both notebooks converted to static HTML with embedded attachments and saved results.

The local host does not allow listening Jupyter sockets. `scripts/execute_notebooks.py` is the tested local fallback. The GitHub Actions workflow independently runs `scripts/execute_with_jupyter.py` with standard Jupyter kernels; view its run for hosted results. This local record does not claim a Google Colab browser session, student-account check or live Gambit/GTE GUI test.

Widget callbacks and parameter wiring were checked. Widget-view payloads are omitted from the committed static output; the source recreates live controls when run in Colab. Code-free teaching pages are generated by `scripts/build_teaching_pages.py` from the actual notebook contents and outputs.
